# Core pipeline dependencies
torch>=2.0.0
transformers>=4.38.0
tqdm>=4.60.0
colorama>=0.4.6
pillow>=9.0.0
opencv-python>=4.7.0.68
pymediainfo>=6.1.0

# NSFW Detection Backends (both, choose as needed)
# NudeNet: (Python <=3.12, NOT 3.13+)
nudenet==2.0.8

# nsfw-detector: (works on 3.11–3.13+, will auto-fallback)
nsfw-detector>=1.2.4
tensorflow-cpu>=2.12.0  # Use tensorflow-cpu for broad compatibility (or tensorflow if you want GPU)

# For YAML config support (optional)
pyyaml>=6.0

# If you want precise versions for maximum stability:
# transformers==4.40.2
# torch==2.2.2
# pillow==10.2.0
